// Description : Programme en sans objet avec variable, if elseif else, >, <, minus, et print, while.
// Resultat : OK

{
    int s;
    int x = 1;
    int t = x || y || z;
    int z = x || y && z || t <= q >= v ;
    int a = x || y && z <= t;
}

