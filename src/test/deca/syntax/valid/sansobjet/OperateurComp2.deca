// Description : Programme en sans objet avec variable et opérateur de comparaison
// Resultat : OK (à comparer avec le fichier .ok)

{
    int s;
    int x = 1;
    int t = x || y || z;
    int z = x || y && z || t <= q >= v ;
    int a = x || y && z <= t;
}

