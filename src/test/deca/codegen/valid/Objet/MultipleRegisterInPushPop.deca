// Description :
//    Test génération de code lordqu'il y a de nombreux registres à push et pop dans un appel de méthode
//
// Resultats decac:
// Resultat ima: 8

//  PROBLEME AVEC IMA:  ** IMA ** ERREUR ** Ligne 38 : 
//    WINT avec R1 adresse code


class A{
    
    int x = 1;

    int TestMultipleRegister(){
        int a, b, c, d, e, f;
	a = 1;
	b = 2;
	c = 3;
	d = 4;
	e = 5;
	f = 6;
        return a*f*b/(c*a*b) + b * (c*b*a*d*e) /(e*(d*a*b)*a);
    }
}
{
    print((new A()).TestMultipleRegister());
    
}
