// Description : Suite de calculs
// Resultat : arbre décoré (.deca.ok)

{
    int x = 5;
    float y = 2.5;
    boolean pos = x > 0;
    y = x + y;
    y = y / 2;
    y = y * 2;
    x = 5 % 3;
    x = -x;
    y = y - 2;
    pos = !pos;
}